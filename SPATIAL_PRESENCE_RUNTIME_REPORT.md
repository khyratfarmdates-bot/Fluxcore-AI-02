# نظام الوعي المكاني والتواجد الذكي (Spatial Presence Runtime)

عولجت مشكلة كون الشخصية مجرد "أيقونة تطفو فوق الشاشة" لتصبح الآن كائناً يقف بثبات على حواف النظام ويستجيب بنعومة لأبعاد النافذة، مع نظام ذكاء استباقي خفيف لا يزعج المستخدم.

## 1. التجسيد الفيزيائي والظلال (Physical Presence & Shadows)
- أضيف **Soft Ground Shadow Plane** أسفل الشخصية بتقنية معتمة بنسبة `15%`، لإعطاء إيحاء بالوزن الجاذبي دون تشويه الواجهة أو النصوص المارة تحتها.
- أضيف **Ambient Contact Glow** بدرجة `AdditiveBlending`، يتغير لونه حسب شعور المساعد: وردي `excited` للفرح، أزرق مضيء للحالة العادية، ورمادي خافت للحالة الخاملة `isIdle/active:false`.

## 2. الإحساس بالعمق والمنظور (Parallax & Depth Illusion)
- تم برمجة محرك `MouseTracker` يتصل بحركة الكاميرا (Head Tracking) ليعطي توجيهاً سلساً لرأس الكائن وعينيه لتبدو دائمًا ناظرة نحو تفاعل المستخدم.
- تمت إضافة وزن افتراضي للكائن عبر تأرجح جيبية (Idle Breathing Range) ليحاكي التنفس بشكل حي وواقعي.

## 3. منطق الاستقرار عند الحواف (Edge Standing Logic)
- تم تحديد هوامش أمان نافذة التطبيق `Math.min(winWidth - 150, Math.max(20, ...))` مما يمنع الشخصية نهائياً من الانزلاق خارج الإطار أو التداخل المدمر مع الأزرار الجانبية.
- يتحرك بوعي نحو المكان المطلوب مع قيود تضمن ألا يتجاوز ارتفاعه حافة الشاشة السفلية أثناء المشي.

## 4. الذكاء البيئي الاستباقي (Ambient Idle Intelligence)
- يعمل مستشعر خاص بحساب فترة الخمول (`Dwell Time` / Inactivity Tracker). إذا تجاوزت 15 ثانية دون تفاعل (نقر، كتابة، تحريك)، ولم تكن الشخصية تشرح شيئًا حالياً:
  - يستيقظ الذكاء ليطرح **رسالة عابرة (Ambient Suggestion)** مثل "هل ترغب في استكشاف أدوات النشر المتقدمة؟".
  - تظهر أزرار تفاعلية مخففة للموافقة أو لتأجيل العرض "لاحقاً".
- لتجنب ظاهرة (Mascot Spam)، تم ربط هذه العملية بنظام مقاطعة (Cooldown Logic `proactiveCooldownUntil`) يعطل المبادرات المتكررة لمدة 60 ثانية بعد أي تنبيه مباشر.

## 5. الأداء (Visual Performance Constraint)
- حلت جميع المؤثرات بمكونات خفيفة خالية من مكلفات المعالجات (Materials بدلاً من Post-processing Layers). 
- الحركات مدمجة بداخل لغة `useFrame` بـ `Three.js` بحيث تعمل بسلاسة عالية 60FPS وتُعلّق تلقائياً إذا كان الإطار الخلفي خارج الرؤية.

---
بهذا التحسين، ارتفع المنتج من مستوى (Widget) عادي إلى مستوى **Premium Companion OS** بتواجد حي مبرمج بلغة جسدية ذكية واستيعاب دقيق لطبيعة حدود الشاشة.
